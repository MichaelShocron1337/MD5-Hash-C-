# MD5-Hash-C-
Implementing a program where the user gets to sign in and the password gets saled + hashed (MD5). Writing to file (database) existning users and hashing their passwords. Used a single file since making several different files didn't make sense because of the shortness of the program.
